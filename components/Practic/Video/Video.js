import s from "./Video.module.scss"

const Video = () => {
    return (
        <section className={s.video}>
            <div className="container">
                <div className={s.title}>
                    Попробуйте вместе со мной, всего за 7 минут
                </div>
                video
            </div>
        </section>
    );
};

export default Video;